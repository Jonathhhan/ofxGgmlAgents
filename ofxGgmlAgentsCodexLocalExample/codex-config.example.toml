# Example only. Copy the relevant blocks into your local Codex config after
# checking the exact keys supported by your installed Codex version.

[model_providers.local_llama]
name = "local-llama"
base_url = "http://127.0.0.1:8001/v1"
wire_api = "responses"

[profiles.ofxggml_local]
model = "unsloth/GLM-4.7-Flash"
model_provider = "local_llama"
